// Main Entry Point
import { engine } from './engine.js';

window.addEventListener('load', () => {
  console.log("NEON STRIKE loading systems...");
  
  // Start the game engine
  engine.start();
});
